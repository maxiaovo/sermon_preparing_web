# VARIABLES.md — Variable & Function Registry

## Rules
- Before creating any variable, function, class, or constant, check this table for naming conflicts.
- Add new entries immediately after creation.
- Scope: `global` (shared across modules) or `module:<name>` (module-local).
- Type: `var`, `const`, `fn`, `class`, `interface`, `type`, `enum`.

## Registry

| Name | Type | Scope | Module | Purpose | Version |
|------|------|-------|--------|---------|---------|
| — | — | — | — | — | — |

## Naming Conventions

- Variables/functions: `camelCase`
- Classes/Components: `PascalCase`
- Constants: `UPPER_SNAKE_CASE`
- Files: `kebab-case`
- CSS classes: `kebab-case` (BEM pattern where appropriate)
